# Tests

- `test_formatting.py`: validates prompt template and normalization helpers.
- Add integration tests for:
  - dataset preprocessing outputs
  - train/eval smoke run on tiny subset
  - API `/generate` contract
